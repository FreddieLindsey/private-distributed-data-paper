dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y make texlive texlive-latex-base texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra biber

test:
  override:
    - make project
    - cp -r project.pdf $CIRCLE_ARTIFACTS/
    - python deploy_to_dropbox.py
